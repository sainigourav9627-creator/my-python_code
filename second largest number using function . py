def second_largest(numbers):
    largest = numbers[0]
    second = numbers[0]

    for num in numbers:
        if num > largest:
            second = largest
            largest = num

    return second


numbers = [10, 25, 7, 40, 15]

result = second_largest(numbers)
print(result)
